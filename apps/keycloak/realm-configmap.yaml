# Realm Keycloak importé au démarrage (--import-realm). Définit :
# - le realm "hackathon"
# - 2 clients OIDC : argocd, grafana (confidentiels)
# - 2 groupes : argocd-admins, grafana-admins (mappés vers les rôles admin)
# - 1 utilisateur démo membre des deux groupes
# NB : secrets de client = valeurs DÉMO (en prod → gérés par ESO).
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-realm
  namespace: keycloak
data:
  hackathon-realm.json: |
    {
      "realm": "hackathon",
      "enabled": true,
      "loginTheme": "keycloak",
      "groups": [
        { "name": "argocd-admins" },
        { "name": "grafana-admins" }
      ],
      "users": [
        {
          "username": "demo",
          "enabled": true, "emailVerified": true, "email": "demo@hackathon.local",
          "firstName": "Demo", "lastName": "SuperAdmin",
          "credentials": [ { "type": "password", "value": "demo", "temporary": false } ],
          "groups": [ "/argocd-admins", "/grafana-admins" ]
        },
        {
          "username": "monitoring",
          "enabled": true, "emailVerified": true, "email": "monitoring@hackathon.local",
          "firstName": "Equipe", "lastName": "Monitoring",
          "credentials": [ { "type": "password", "value": "monitoring", "temporary": false } ],
          "groups": [ "/grafana-admins" ]
        },
        {
          "username": "appweb",
          "enabled": true, "emailVerified": true, "email": "appweb@hackathon.local",
          "firstName": "Equipe", "lastName": "AppWeb",
          "credentials": [ { "type": "password", "value": "appweb", "temporary": false } ],
          "groups": [ "/argocd-admins" ]
        }
      ],
      "clients": [
        {
          "clientId": "argocd",
          "enabled": true,
          "protocol": "openid-connect",
          "publicClient": false,
          "secret": "argocd-oidc-demo-secret",
          "standardFlowEnabled": true,
          "directAccessGrantsEnabled": true,
          "redirectUris": [
            "https://localhost:8080/*", "http://localhost:8080/*",
            "https://localhost:8081/*", "http://localhost:8081/*",
            "https://localhost:8083/*", "http://localhost:8083/*"
          ],
          "webOrigins": [ "*" ],
          "protocolMappers": [
            {
              "name": "groups",
              "protocol": "openid-connect",
              "protocolMapper": "oidc-group-membership-mapper",
              "config": {
                "full.path": "false",
                "id.token.claim": "true",
                "access.token.claim": "true",
                "claim.name": "groups",
                "userinfo.token.claim": "true"
              }
            }
          ]
        },
        {
          "clientId": "grafana",
          "enabled": true,
          "protocol": "openid-connect",
          "publicClient": false,
          "secret": "grafana-oidc-demo-secret",
          "standardFlowEnabled": true,
          "redirectUris": [
            "https://localhost:3000/*", "http://localhost:3000/*",
            "https://localhost:3001/*", "http://localhost:3001/*"
          ],
          "webOrigins": [ "*" ],
          "protocolMappers": [
            {
              "name": "groups",
              "protocol": "openid-connect",
              "protocolMapper": "oidc-group-membership-mapper",
              "config": {
                "full.path": "false",
                "id.token.claim": "true",
                "access.token.claim": "true",
                "claim.name": "groups",
                "userinfo.token.claim": "true"
              }
            }
          ]
        }
      ]
    }
